<?php namespace App\Http\Controllers; use Illuminate\Http\Request; use App\tampilans; class tampilanController extends Controller {public function __construct() {$this->middleware('auth'); } public function tampil() {$tampilans = tampilans::find(1); return view('tampilan',['tampilans'=>$tampilans]); } public function tambah(Request $request) {$tampilans = new tampilans; $tampilans->favicon = $request->favicon; $tampilans->logo = $request->logo; $tampilans->kesbangpol = $request->kesbangpol; $tampilans->site_title = $request->site_title; $tampilans->site_keyword = $request->site_keyword; $tampilans->site_desc = $request->site_desc; $tampilans->alamat = $request->alamat; $tampilans->no_hp = $request->no_hp; $tampilans->email = $request->email; $tampilans->jam_buka = $request->jam_buka; $tampilans->jam_tutup = $request->jam_tutup; $tampilans->facebook = $request->facebook; $tampilans->twitter = $request->twitter; $tampilans->instagram = $request->instagram; $tampilans->save(); return redirect()->action('tampilanController@tampil'); } public function ubah(Request $request) {$tampilans = tampilans::find(1); $tampilans->favicon = $request->favicon; $tampilans->logo = $request->logo; $tampilans->kesbangpol = $request->kesbangpol; $tampilans->site_title = $request->site_title; $tampilans->site_keyword = $request->site_keyword; $tampilans->site_desc = $request->site_desc; $tampilans->alamat = $request->alamat; $tampilans->no_hp = $request->no_hp; $tampilans->email = $request->email; $tampilans->jam_buka = $request->jam_buka; $tampilans->jam_tutup = $request->jam_tutup; $tampilans->facebook = $request->facebook; $tampilans->twitter = $request->twitter; $tampilans->instagram = $request->instagram; $tampilans->save(); return redirect()->action('tampilanController@tampil'); } }